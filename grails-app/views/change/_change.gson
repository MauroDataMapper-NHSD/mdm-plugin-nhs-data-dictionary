import uk.nhs.digital.maurodatamapper.datadictionary.publish.changePaper.Change

model {
    Change change
}
json {

    changeType change.getChangeType()
    stereotype change.stereotype
    detail change.htmlDetail

    //g.render (template: model: change.oldItem)
    //newItem g.render (change.newItem)

}