import uk.nhs.digital.maurodatamapper.datadictionary.rewrite.integritychecks.IntegrityCheck

model {
    IntegrityCheck integrityCheck
}

json {
    name integrityCheck.name
    description integrityCheck.description
    errors g.render(integrityCheck.errors ?: [])

    size integrityCheck.errors.size()
}